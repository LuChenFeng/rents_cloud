<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.lcf.rents.forum.mapper.UserMapper">
    <select id="findByUserName" parameterType="String" resultType="pers.lcf.rents.forum.model.User">
          SELECT * FROM MyUser where name=#{name}
    </select>
</mapper>